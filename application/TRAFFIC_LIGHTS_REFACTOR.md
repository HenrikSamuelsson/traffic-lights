# Traffic Lights Refactor — LED Component + Composite

This adds:
- `led.h` / `led.c`
- `traffic_light.h` / `traffic_light.c`

Implement `platform_gpio_write/read` in `led.c` for your HAL (e.g., Zephyr, nRF SDK).

Headers in: `src`
Sources in: `src`
